import * as React from "react";
import preloader from "../../assets/images/d2.gif";

const Preloader = () => {
    return (
        <div>
            <img src= {preloader} alt="preloaderImg"/>
        </div>
    );
};

export default Preloader;
